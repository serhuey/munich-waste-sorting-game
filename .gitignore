# Snapshots of the Abfalllexikon are versioned as JSON — the diff needs history.
# The raw HTML is a debugging aid only, and it is large.
data/verified/raw-*.html

__pycache__/
*.py[cod]
.DS_Store

# Личные данные для Impressum: на сайте они публичны, в истории коммитов им не место.
legal/values.local.json

# Собранная копия: в ней подставлены личные данные, и её место — репозиторий сайта.
dist/
